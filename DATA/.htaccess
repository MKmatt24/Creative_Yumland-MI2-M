# Interdire l'accès direct aux fichiers de données depuis le navigateur
# Compatible Apache 2.2 et 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>